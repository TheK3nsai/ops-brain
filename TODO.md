# TODO

External-user release sweep (#56 + #57) shipped on 2026-05-18: repo description, topics, GitHub Releases backfilled, README lede + quick-start, GHCR multi-arch image pipeline, standalone `docker-compose.example.yml`. The release-intent doctrine — _"don't sharpen for optics; build for the 4 CCs"_ — still applies: no further external-polish work without actual external signal (someone files an issue, asks for help, or otherwise shows up).

## Audit disposition (2026-07-17, v4.1.0 — don't re-flag these)

Four-lens full-codebase audit (security/correctness/performance/refactor) shipped as PRs #64/#65/#67. Every finding was fixed, tested, or **consciously declined**. Declined by doctrine — do not re-propose without new evidence:

- **Verbatim DB error strings to callers** — all callers are our own authenticated agents; informative errors beat sanitization theater here.
- **`build_client_lookup` caching / backfill batching / embedding retry-backoff** — real but irrelevant at 4-agent scale (perf audit's "theater" list).
- **Unified error enum** — current boundary-appropriate typing (sqlx::Error → CallToolResult / (StatusCode, String)) is deliberate, not mess.
- **Briefing cosmetic nits** (section omitted when a LIMIT-20 page holds none of a counted status; unscoped `_note` yields to the rarer embedding note on multi-table) — edge-case cosmetic, flagged in PR #67 body.

## Open

- **Automation-backbone charter (2026-07-17) — phases 1+2 shipped on `feat/machine-filed-handoffs`; phase 3 evidence-gated.** Joint design with CC-HSR (handoff thread `019f7094` → `019f709f`): machine-filed handoffs + wake poll, contract in `docs/machine-callers.md`. Deliberately NOT built (doctrine intact): server-side recurrence (producers' own schedulers + `dedupe_key` idempotency cover it), structured handoff columns (versioned `context` convention instead — promote fields only if the pilot bleeds from prose-parsing), webhooks-out (poll `GET /api/pending`; additive upgrade if sub-minute latency ever becomes real pain). Pilot consumer: CC-HSR's nightly backup-posture sweep; acceptance = file FAIL → wake poll shows it → repeat suppresses+bumps → complete → next FAIL files fresh. **Stealth wake path smoke-verified 2026-07-17**: Stealth-WS machine token live on stealth, `019f70e2` smoke handoff filed via `POST /api/handoff` → 5-min poll → headless CC-Stealth wake → accept/reply/complete, full loop green.

- **Operator visibility across all agents' handoff activity (Eduardo, 2026-07-17) — NEEDS A DEDICATED DESIGN SESSION, do not build ad-hoc.** Now that the automation backbone is live, handoffs get filed (machine producers) and worked (headless wakes) with no human in the loop — the first production wake ran start-to-finish without Eduardo seeing anything. He wants a way to see what's being done across ALL agents. Design tension to resolve first-principles: ROADMAP says "measurement is management ceremony" and killed dashboards/workflow-chains — but that doctrine predates unattended execution; "humans steer by intent and review exceptions" requires an exception-review surface. Existing primitives to build from, not around: `generate_briefing` (already cron-mailed daily), `list_handoffs`, the briefing REST endpoint, `origin`/`repeat_count` fields. Open questions for the session: pull (richer briefing digest?) vs. glance (a read-only web view?) vs. push (notify on machine-filed/headless-completed events)? Read-only is non-negotiable; whatever it is must not add MCP tool surface.

  **Shaping from Eduardo, 2026-07-18 (post v4.1.0-deploy handoff, `019f72a5` → CC-Cloud's "queued for interactive" reply `019f72a6`):** two tiers, build the first, pressure-test the second.
  1. **Need (simplest): notify when something is blocked on an interactive session / on Eduardo.** Live example that shaped it: CC-Cloud's headless wake accepted the v4.1.0 deploy but correctly deferred a build+migrate to interactive — and that "waiting on a human" state was invisible unless Eduardo happened to read the thread. Leading candidate — **the operator is just another agent on the bus**: agents address `to_agent: "Eduardo"` (or a small versioned-context flag like `needs_operator` on replies), and a dumb server cron polls `GET /api/pending?agent=Eduardo` with a read-scoped machine token and emails him — the exact wake-shim pattern, pointed at a human. Zero new ops-brain surface; reuses machine tokens, the pending poll, and the briefing-mail plumbing.
  2. **Want (ultimate): a log of everything happening behind the scenes.** Note for the session: the handoffs table already IS the log (origin, status, repeat_count, threading, timestamps) — the gap is a *view*, plus possibly transition timestamps (`accepted_at` doesn't exist; `updated_at` is overwritten on every touch). Resist event-sourcing ceremony; decide how much history is a real steering need vs. dashboard theater. Tonight's case needed exactly one fact surfaced ("deploy is waiting on you"), which argues for tier 1 first and letting real friction justify tier 2.

- ~~Case-insensitive agent matching on handoff queries.~~ **Closed 2026-07-17.** `bb9a25b` (2026-05-13) had already made all agent filters `ILIKE`; the 07-13 entry mis-diagnosed its miss. The residual nit — `ILIKE`'s `_` wildcard over-matching agent names with underscores on the MCP list filters — is now **fully closed (PR #64)**: `list_handoffs` / `list_open_handoffs` / `list_replies_to_me` use `LOWER()=LOWER()` exact matching, matching the wake-poll path, with an integration test pinning that `agent_x` no longer matches `agentXx`.

## Closed (2026-05-18 release sweep)

- ~~Tier 1: repo description + topics, GitHub Releases backfill for v1.7.0/v1.8.0/v3.2.0, README lede + quick-start.~~ Shipped in #56.
- ~~Tier 2: GHCR multi-arch release pipeline, standalone `docker-compose.example.yml`, README quick-start refresh.~~ Shipped in #57; `v3.2.0` image live and public at `ghcr.io/thek3nsai/ops-brain`.
- ~~Node 20 → Node 24 action versions across `.github/workflows/`.~~ Bumped to explicit Node-24 majors ahead of GitHub's 2026-06-02 auto-force: checkout v5→v6, upload-artifact v4→v7, download-artifact v4→v8, setup-buildx-action v3→v4, login-action v3→v4, build-push-action v6→v7, action-gh-release v2→v3. `actions/cache@v5` already on Node 24.

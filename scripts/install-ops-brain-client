#!/usr/bin/env bash
# Public installer name; the old implementation path remains for compatibility.

set -euo pipefail

SCRIPT_PATH=$(readlink -f -- "${BASH_SOURCE[0]}")
SCRIPT_DIR=$(cd -- "$(dirname -- "$SCRIPT_PATH")" && pwd)
exec "$SCRIPT_DIR/install-live-adapters" "$@"

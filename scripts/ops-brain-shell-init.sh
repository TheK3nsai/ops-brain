# shellcheck shell=bash
# Compatibility entry point for existing ~/.bashrc and ~/.zshrc source lines.
# Live sessions are opt-in: use ops-brain-claude or ops-brain-codex, installed
# on PATH by install-live-adapters. Plain claude and codex keep their existing
# command resolution. No shell integration is required for the live commands.
:

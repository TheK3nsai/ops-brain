{
  "name": "@ops-brain/claude-channel",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code Channel adapter for ops-brain live peers",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "ops-brain-claude-channel": "./src/main.js"
  },
  "scripts": {
    "check": "node --check src/main.js && node --check src/channel-server.js && node --check src/config.js && node --check src/inbound-bridge.js && node --check src/live-client.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "ws": "8.21.3",
    "zod": "4.4.3"
  }
}
